
### 製作Dockerfile

**Dockerfile**

workdir資料夾中有打包完成的JAVA專案jar檔`demo-0.0.1-SNAPSHOT.jar`，及未完成的`Dockerfile`。

請依指示完成Dockerfile撰寫:

1. base image: `openjdk:17`
2. jar檔放置路徑:  `/opt/app`
3. java執行指令: java -jar demo-0.0.1-SNAPSHOT.jar

